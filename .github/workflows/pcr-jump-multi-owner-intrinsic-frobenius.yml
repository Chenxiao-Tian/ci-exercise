name: PCR Jump intrinsic multi-owner Frobenius chamber

on:
  push:
    branches:
      - pcr-jump-lean-20260802-u2-root-boundary
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pcr-jump-intrinsic-multi-owner-frobenius-${{ github.sha }}
  cancel-in-progress: false

jobs:
  intrinsic-multi-owner-frobenius-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install pinned Lean
        run: |
          set -o pipefail
          curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf \
            | sh -s -- -y --default-toolchain leanprover/lean4:v4.30.0
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Resolve pinned mathlib
        run: |
          set -o pipefail
          lake update
          lake exe cache get
      - name: Reject placeholders
        run: |
          set -o pipefail
          if grep -nE '^[[:space:]]*(axiom|sorry|admit)([[:space:]]|$)' \
              PCRLean/MultiOwnerHasseCore.lean \
              PCRLean/Experimental/IntrinsicKernelIdealFunctoriality.lean \
              PCRLean/Experimental/IntrinsicKernelRootPacket.lean \
              PCRLean/Experimental/FiniteDimensionalSymmetricRegularBridge.lean \
              PCRLean/Experimental/SurjectiveFiniteDimensionalRegularCentre.lean \
              PCRLean/Experimental/ArbitraryFiniteLinearPacketRegularCentre.lean \
              PCRLean/Experimental/MultiOwnerIntrinsicFrobeniusChamberV2.lean \
              PCRLean/Experimental/MultiOwnerIntrinsicFrobeniusChamberV2Audit.lean; then
            echo 'Forbidden proof placeholder or custom axiom found.' >&2
            exit 1
          fi
      - name: Build intrinsic multi-owner Frobenius chamber
        run: |
          set -o pipefail
          lake build PCRLean.Experimental.MultiOwnerIntrinsicFrobeniusChamberV2 2>&1 \
            | tee intrinsic-multi-owner-frobenius-build.txt
      - name: Kernel axiom audit
        run: |
          set -o pipefail
          lake env lean PCRLean/Experimental/MultiOwnerIntrinsicFrobeniusChamberV2Audit.lean 2>&1 \
            | tee intrinsic-multi-owner-frobenius-axioms.txt
          if grep -E 'sorryAx|PCRLean\..*Axiom|ResolutionAxiom|PacketizationAxiom' \
              intrinsic-multi-owner-frobenius-axioms.txt; then
            echo 'Unpermitted axiom detected.' >&2
            exit 1
          fi
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pcr-jump-intrinsic-multi-owner-frobenius-${{ github.sha }}
          path: |
            intrinsic-multi-owner-frobenius-build.txt
            intrinsic-multi-owner-frobenius-axioms.txt
          if-no-files-found: warn
