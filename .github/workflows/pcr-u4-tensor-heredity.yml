name: PCR U4 split-centre tensor heredity

on:
  push:
    branches:
      - pcr-u4-tensor-heredity-20260803
  pull_request:
    branches:
      - pcr-u4-tensor-heredity-base-20260803
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pcr-u4-tensor-heredity-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  tensor-heredity-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install pinned Lean
        run: |
          set -o pipefail
          curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf \
            | sh -s -- -y --default-toolchain leanprover/lean4:v4.30.0
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Resolve pinned mathlib
        run: |
          set -o pipefail
          lake update
          lake exe cache get
      - name: Reject placeholders
        run: |
          set -o pipefail
          if grep -nE '^[[:space:]]*(axiom|sorry|admit)([[:space:]]|$)' \
              PCRLean/Experimental/SplitSurjectionTensorHeredity.lean \
              PCRLean/Experimental/SplitSurjectionTensorHeredityAudit.lean; then
            echo 'Forbidden proof placeholder or custom axiom found.' >&2
            exit 1
          fi
      - name: Build tensor-heredity theorem
        run: |
          set -o pipefail
          lake build PCRLean.Experimental.SplitSurjectionTensorHeredity 2>&1 \
            | tee tensor-heredity-build.txt
      - name: Kernel axiom audit
        run: |
          set -o pipefail
          lake env lean PCRLean/Experimental/SplitSurjectionTensorHeredityAudit.lean 2>&1 \
            | tee tensor-heredity-axioms.txt
          if grep -E 'sorryAx|PCRLean\..*Axiom|ResolutionAxiom|PacketizationAxiom' \
              tensor-heredity-axioms.txt; then
            echo 'Unpermitted axiom detected.' >&2
            exit 1
          fi
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pcr-u4-tensor-heredity-${{ github.sha }}
          path: |
            tensor-heredity-build.txt
            tensor-heredity-axioms.txt
          if-no-files-found: warn
