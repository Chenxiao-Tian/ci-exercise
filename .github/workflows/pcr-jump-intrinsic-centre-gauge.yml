name: PCR Jump intrinsic centre gauge invariance

on:
  push:
    branches:
      - pcr-jump-lean-20260802-u2-root-boundary
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pcr-jump-intrinsic-centre-gauge-${{ github.sha }}
  cancel-in-progress: false

jobs:
  intrinsic-centre-gauge-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install pinned Lean
        run: |
          set -o pipefail
          curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf \
            | sh -s -- -y --default-toolchain leanprover/lean4:v4.30.0
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Resolve pinned mathlib
        run: |
          set -o pipefail
          lake update
          lake exe cache get
      - name: Reject placeholders
        run: |
          set -o pipefail
          if grep -nE '^[[:space:]]*(axiom|sorry|admit)([[:space:]]|$)' \
              PCRLean/Experimental/SurjectiveLinearMapSymmetricQuotient.lean \
              PCRLean/Experimental/IntrinsicCentreGaugeInvariance.lean \
              PCRLean/Experimental/IntrinsicCentreGaugeInvarianceAudit.lean; then
            echo 'Forbidden proof placeholder or custom axiom found.' >&2
            exit 1
          fi
      - name: Build intrinsic centre gauge invariance
        run: |
          set -o pipefail
          lake build PCRLean.Experimental.IntrinsicCentreGaugeInvariance 2>&1 \
            | tee intrinsic-centre-gauge-build.txt
      - name: Kernel axiom audit
        run: |
          set -o pipefail
          lake env lean PCRLean/Experimental/IntrinsicCentreGaugeInvarianceAudit.lean 2>&1 \
            | tee intrinsic-centre-gauge-axioms.txt
          if grep -E 'sorryAx|PCRLean\..*Axiom|ResolutionAxiom|PacketizationAxiom' \
              intrinsic-centre-gauge-axioms.txt; then
            echo 'Unpermitted axiom detected.' >&2
            exit 1
          fi
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pcr-jump-intrinsic-centre-gauge-${{ github.sha }}
          path: |
            intrinsic-centre-gauge-build.txt
            intrinsic-centre-gauge-axioms.txt
          if-no-files-found: warn
