name: PCR Jump linear split-centre quotient

on:
  push:
    branches:
      - pcr-jump-lean-20260802-u2-root-boundary
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pcr-jump-linear-split-centre-${{ github.sha }}
  cancel-in-progress: false

jobs:
  linear-split-centre-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install pinned Lean
        run: |
          set -o pipefail
          curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf \
            | sh -s -- -y --default-toolchain leanprover/lean4:v4.30.0
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Resolve pinned mathlib
        run: |
          set -o pipefail
          lake update
          lake exe cache get
      - name: Reject placeholders
        run: |
          set -o pipefail
          if grep -nE '^[[:space:]]*(axiom|sorry|admit)([[:space:]]|$)' \
              PCRLean/Experimental/CoordinateCentreQuotient.lean \
              PCRLean/Experimental/LinearSplitCentreQuotient.lean \
              PCRLean/Experimental/LinearSplitCentreQuotientAudit.lean; then
            echo 'Forbidden proof placeholder or custom axiom found.' >&2
            exit 1
          fi
      - name: Build linear split-centre bridge
        run: |
          set -o pipefail
          lake build PCRLean.Experimental.LinearSplitCentreQuotient 2>&1 \
            | tee linear-split-centre-build.txt
      - name: Kernel axiom audit
        run: |
          set -o pipefail
          lake env lean PCRLean/Experimental/LinearSplitCentreQuotientAudit.lean 2>&1 \
            | tee linear-split-centre-axioms.txt
          if grep -E 'sorryAx|PCRLean\..*Axiom|ResolutionAxiom|PacketizationAxiom' \
              linear-split-centre-axioms.txt; then
            echo 'Unpermitted axiom detected.' >&2
            exit 1
          fi
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pcr-jump-linear-split-centre-${{ github.sha }}
          path: |
            linear-split-centre-build.txt
            linear-split-centre-axioms.txt
          if-no-files-found: warn
