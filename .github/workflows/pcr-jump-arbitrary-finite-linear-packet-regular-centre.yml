name: PCR Jump arbitrary finite linear-packet regular centre

on:
  push:
    branches:
      - pcr-jump-lean-20260802-u2-root-boundary
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pcr-jump-arbitrary-finite-linear-packet-regular-centre-${{ github.sha }}
  cancel-in-progress: false

jobs:
  arbitrary-finite-linear-packet-regular-centre-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install pinned Lean
        run: |
          set -o pipefail
          curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf \
            | sh -s -- -y --default-toolchain leanprover/lean4:v4.30.0
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Resolve pinned mathlib
        run: |
          set -o pipefail
          lake update
          lake exe cache get
      - name: Reject placeholders
        run: |
          set -o pipefail
          if grep -nE '^[[:space:]]*(axiom|sorry|admit)([[:space:]]|$)' \
              PCRLean/Experimental/IntrinsicKernelIdealFunctoriality.lean \
              PCRLean/Experimental/FiniteDimensionalSymmetricRegularBridge.lean \
              PCRLean/Experimental/FiniteDimensionalIntrinsicRegularCentre.lean \
              PCRLean/Experimental/SurjectiveFiniteDimensionalRegularCentre.lean \
              PCRLean/Experimental/ArbitraryFiniteLinearPacketRegularCentre.lean \
              PCRLean/Experimental/ArbitraryFiniteLinearPacketRegularCentreAudit.lean; then
            echo 'Forbidden proof placeholder or custom axiom found.' >&2
            exit 1
          fi
      - name: Build arbitrary finite linear-packet regular centre
        run: |
          set -o pipefail
          lake build PCRLean.Experimental.ArbitraryFiniteLinearPacketRegularCentre 2>&1 \
            | tee arbitrary-finite-linear-packet-regular-centre-build.txt
      - name: Kernel axiom audit
        run: |
          set -o pipefail
          lake env lean PCRLean/Experimental/ArbitraryFiniteLinearPacketRegularCentreAudit.lean 2>&1 \
            | tee arbitrary-finite-linear-packet-regular-centre-axioms.txt
          if grep -E 'sorryAx|PCRLean\..*Axiom|ResolutionAxiom|PacketizationAxiom' \
              arbitrary-finite-linear-packet-regular-centre-axioms.txt; then
            echo 'Unpermitted axiom detected.' >&2
            exit 1
          fi
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pcr-jump-arbitrary-finite-linear-packet-regular-centre-${{ github.sha }}
          path: |
            arbitrary-finite-linear-packet-regular-centre-build.txt
            arbitrary-finite-linear-packet-regular-centre-axioms.txt
          if-no-files-found: warn
