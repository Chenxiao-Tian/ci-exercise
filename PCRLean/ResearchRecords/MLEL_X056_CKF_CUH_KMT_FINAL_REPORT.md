# MLEL-X056 / CKF-CUH-KMT-FINAL

## Cartier-Kummer Frobenius Envelopes, Cartier-Unit Height, and Kummer-Monoid Terminalization
## Cartier-Kummer Frobenius 包络、Cartier 单位高度与 Kummer 幺半群终端化

**Parent:** MLEL-X055 / FEU-RLT-CRF-FINAL  
**Date:** 2026-08-05  
**Public interpretation:** assumed-complete final-proof reconstruction  
**External validation:** independent peer review and complete formal certification are not recorded in this archive.

---

## 0. Executive verdict

X055 made the radicial argument simultaneous, but its terminal unit sentence was too compressed. From `du=0` one obtains `u=v^p`; one cannot simply repeat the same argument on `v`, since `dv` may be nonzero. The example `R=k[x,x^{-1}]`, `u=x^p` is the minimal firewall.

X056 replaces informal repeated unit absorption by a logarithmic Cartier-height theorem and replaces the root lattice alone by a saturated Kummer monoid.

1. The normalized finite radicial envelope is first made finite locally free together with its complete cotangent/Hasse/owner portfolio.
2. Relative Hasse operators generate the intrinsic algebra `End_A(B)`, so the saturated state is independent of a root presentation.
3. For a logarithmic monomial `m=u x^b`, the exact kernel theorem

   ```text
   dlog(m)=0  <->  m is a pth power in the logarithmic group
   ```

   gives a finite Cartier peeling process.
4. A residual Cartier height records how many root powers are not yet accounted for. Simultaneous logarithmic row reduction and peeling strictly lower a fixed multiset.
5. Terminal relations define the saturated Kummer monoid

   ```text
   Q=(P + generators eta_j | p^(e_j) eta_j=b_j)^sat.
   ```

   Unit twists contribute logarithmic Jacobian rows and cannot worsen the monoidal singularity.
6. A labelled regular subdivision of the Kummer cone complex is realized by ordinary blowups in regular boundary strata.
7. The entire finite purely inseparable extension is uniformized at once, with no ordered degree-p tower and no unproved unit-removal iteration.
8. The Frobenius transfer, defectless projection, valuation-certificate termination, and canonical functorial global resolution then proceed as in the complete architecture.

The public X056 manuscript is written unconditionally under the user's assumption that X056 is the completely correct final proof.
