# MLEL-X20260803 / SNC-Strengthened Graded Frobenius Graph-Macro Frontier

**Date:** 2026-08-03  
**Protocol:** `PCR-JUMP-LEAN 2.0 / MLEL-A002-JUMP2`  
**Accepted certified parent:** `MLEL-M004 / HMC-SCCD`  
**Experimental branch:** `pcr-jump-integral-heredity-20260803`  
**Experimental dependency-index head:** `2fd1b04b47915745e189b92859688436968f9466`  
**Exact clean-room PR:** `#25`  
**Clean-room run:** `30839499119`  
**Status at synchronization:** `queued`  
**Promoted to Certified Graph:** `false`  
**Global status:** `OPEN_GAP`

This record archives the complete current conversation output without promoting experimental theorems.

## Permanent structural corrections

The research line distinguishes:

- cotangent row-span ideals from tangent annihilators;
- actual ideal, properness, nonwhole-centre strictness, regular quotient, regular immersion, marked permissibility, singular-locus containment and nonidentity action;
- active containment, passive Tor/normal-flat safety and boundary SNC;
- presentation normalization from ordinary blowup;
- source-conservative cleanup from genuine birth.

Retained no-go examples include identity packets, principal Cartier identity actions, owner-union kernel collapse, nonreduced normal-cone failure, imperfect-field coefficient obstruction and the hybrid cusp `y^p-x^(p+1)`.

## Strongest experimental local chamber

For a Noetherian regular coefficient domain `R` of characteristic `p`, a finite nonempty graph centre

```text
I_h = (Z_i-h_i) in R[Z_i],
```

a root block `q=p^e`, `0 < mark <= q`, an `R`-flat passive module, a finite boundary family and a finite source ledger, the dependency graph assembles:

1. actual proper nonzero finite-type centre;
2. exact quotient `R[Z_i]/I_h ≃ R` and regular centre ring;
3. Frobenius-normal centre filtration;
4. active marked permissibility at every positive mark at most q;
5. every standard chart and exact factorization;
6. residual ideal exactly `(E_k)^(q-mark)`;
7. terminality at `mark=q`;
8. induced-flat passive Tor safety along every centre power;
9. flat restriction of passive data to the centre;
10. exact boundary intersection quotient `R[Z_i]/(I_h+J_P) ≃ R/J`;
11. regular intersection with every finite coefficient-boundary stratum;
12. exact preservation and reflection of ordered boundary regular sequences;
13. source support and active identity count preserved;
14. cleanup height `q -> q-mark`, hence strict concrete `GenRank` decrease.

The integrated module is:

```text
PCRLean.Experimental.PolynomialGraphLocalResolutionMacro
```

with unified index and audit:

```text
PCRLean.Experimental.GradedFrobeniusHeredityKernelIndex
PCRLean/Experimental/GradedFrobeniusHeredityKernelIndexAudit.lean
```

## Boundary/SNC strengthening

The graph quotient equivalence transports coefficient boundary equations. The experimental declarations prove:

```text
IsWeaklyRegular on R[Z]/I_h  <->  IsWeaklyRegular on R
IsRegular on R[Z]/I_h        <->  IsRegular on R.
```

Thus the local boundary certificate contains both regularity of every finite stratum and the non-zero-divisor/codimension component of the SNC gate. Global divisor identities, units/permutations on overlaps and chartwise boundary transport remain open.

## Evidence boundary

The source has exact definitions, proof-term candidates, counterexample audits and `#print axioms` targets. Clean-room PR #25, run `30839499119`, was queued at synchronization. Correct label:

```text
EXPERIMENTAL-WRITTEN-CLEANROOM-PENDING
```

It is not kernel verified, not imported by `CertifiedIndex` and not a general resolution theorem.

## Known geometry and formal burden

Over a perfect field, a regular finite-type centre is smooth, and a closed immersion between smooth schemes is étale-locally a coordinate subspace. After an algorithm constructs an actual regular centre, local graph/coordinate models are therefore standard mathematics. Remaining work is formal scheme-level atlas construction, exact ideal/chart/history descent and hereditary state reconstruction.

## Highest-posterior architecture

```text
intrinsic finite Frobenius–Hasse packet
-> graded Frobenius normalization and ceiling marks
-> cotangent/conormal or canonical hybrid centre synthesis
-> actual regular centre or finite centre word
-> etale polynomial-graph atlas
-> local graph macro: active + induced-flat passive
   + regular boundary strata + regular boundary sequences
   + every chart + source-conservative cleanup
-> hereditary next-packet reconstruction without reset
-> source realization for genuine births
-> projection-free immediate-defect escape
-> Noetherian/multiset/degree/debt causal termination
-> finite functorial globalization
-> principalization and resolution
```

## Remaining frontiers

1. universal finite intrinsic packet extraction and presentation independence;
2. universal actual-centre synthesis, including hybrid/rank-zero/nonlinear cases;
3. scheme-level étale graph atlas and exact ideal/chart gluing;
4. arbitrary passive modules, full normal flatness and global SNC/divisor gluing;
5. hereditary next-packet reconstruction after every chart and normalization;
6. geometric source supports for genuine births, with no split or clone;
7. projection-free immediate-defect carrier;
8. finite global serialization and final principalization/resolution compilers.

```text
CURRENT_CERTIFIED_NODE                     = MLEL-M004 / HMC-SCCD
LATEST_EXPERIMENTAL_SNC_MACRO_WRITTEN      = true
LATEST_EXPERIMENTAL_CLEANROOM_GREEN        = false
LATEST_EXPERIMENTAL_RESULTS_PROMOTED       = false
FINAL_MAIN_THEOREM_KERNEL_VERIFIED         = false
GENERAL_POSITIVE_CHARACTERISTIC_RESOLUTION = false
FORMAL_GLOBAL_STATUS                       = OPEN_GAP
```