# X035 Lake module-target clean-room retrigger

The workflow now uses Lake module targets so dependency `.olean` files are
created before downstream imports. This head adds only this trigger marker. No
theorem is promoted by this pull request.
