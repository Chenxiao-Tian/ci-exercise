# Corrected fpqc image-centre clean-room trigger

The base branch contains the corrected mathematical source snapshot and the
clean-room workflow.  This head differs only by this trigger marker.

The run is experimental.  No declaration is promoted into the official
MLE–Lean Certified Graph, and the global truth state remains `OPEN_GAP`.
