# X039 clean-room retrigger after explicit parameter repair

The Hironaka tensor-flatness compiler now supplies the base ring, module
families, and certificate parameters explicitly to the mathlib flatness API.
This head adds only the trigger marker. No declaration is promoted by the pull
request.
